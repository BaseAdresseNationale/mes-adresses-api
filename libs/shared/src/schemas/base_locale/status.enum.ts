export enum StatusBaseLocalEnum {
  DRAFT = 'draft',
  READY_TO_PUBLISH = 'ready-to-publish',
  PUBLISHED = 'published',
  DEMO = 'demo',
  REPLACED = 'replaced',
}

export enum StatusSyncEnum {
  OUTDATED = 'outdated',
  SYNCED = 'synced',
  CONFLICT = 'conflict',
}
